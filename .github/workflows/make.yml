name: C/C++ CI

on: [ push, pull_request]

jobs:
  build:

    runs-on: [ubuntu-latest, macos-latest]

    steps:
    - uses: actions/checkout@v3
    - name: make
      run: cd src ; make
    - name: test
      run: cd src ; ./kerf_test
